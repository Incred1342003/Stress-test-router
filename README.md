## Installation Process
 - on respberry pi,
 - clone the repo using url `https://github.com/panditpankaj21/Stress-test-router.git`
 - do `pip install behave`
 - do `pip install -r requirements.txt`
 - set your's the interface, router ip 
 - run cmmd `sudo behave`